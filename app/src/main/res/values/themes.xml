<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.IntershalaAssignment" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.IntershalaAssignment" parent="Base.Theme.IntershalaAssignment" />

    <style name="Base.Theme.IntershalaAssignment2" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>


    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
    <item name = "windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/github_mark_white</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.IntershalaAssignment</item>
    </style>

    <style name="roundImage" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">10dp</item>
        <item name="android:radius">20dp</item>
        <item name="color">@color/white</item>
    </style>

</resources>